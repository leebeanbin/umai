# Web
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
uvloop>=0.19.0          # 고성능 이벤트 루프 (Linux/macOS)
httptools>=0.6.0        # 고성능 HTTP 파서
pydantic[email]>=2.0.0
pydantic-settings>=2.0.0
python-multipart

# Database (async PostgreSQL)
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0

# Redis
redis[hiredis]>=5.0.0

# Kafka
aiokafka>=0.11.0

# Auth / Security
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
authlib>=1.3.0          # OAuth (Google, GitHub)
httpx>=0.27.0           # OAuth HTTP client
itsdangerous>=2.1.0     # Starlette SessionMiddleware
slowapi>=0.1.9          # FastAPI rate limiting

# HTTP (LLM proxy)
httpx>=0.27.0
